# Runtime dependencies for the application itself.
-r ../../requirements.txt

# Packaging-only dependency used by:
# - release/windows-portable/build_windows.bat
# - scripts/prepare_windows_offline_wheels.py
pyinstaller>=6.0,<7.0

# Windows-only transitive dependencies that pip may skip when the wheelhouse is
# prepared from a non-Windows host. Keep them explicit so offline packaging on
# Windows remains self-contained.
pefile>=2022.5.30
pywin32-ctypes>=0.2.1
pywin32>=306
